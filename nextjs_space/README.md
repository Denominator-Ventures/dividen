# DiviDen

**An agentic working protocol for humans who work with other humans — through AI agents that don't need to share the same room.**

---

## Quick Start (5 minutes)

### Prerequisites

| Requirement | Version | How to Get It |
|---|---|---|
| **Node.js** | 18+ (22 recommended) | [nodejs.org](https://nodejs.org) |
| **PostgreSQL** | Any | Local via Docker (included), or hosted (Neon, Supabase, Railway) |

> **Docker is 100% optional.** We include a `docker-compose.yml` to spin up a local Postgres with one command. If you already have Postgres, skip Docker entirely.

### One-Command Setup

```bash
git clone https://github.com/Denominator-Ventures/dividen.git
cd dividen/nextjs_space
```

**macOS / Linux / WSL:**
```bash
bash scripts/setup.sh
```

**Windows (PowerShell):**
```powershell
.\scripts\setup.ps1
```

That's it. The setup script will:
1. ✅ Install dependencies (yarn or npm — either works)
2. ✅ Create `.env` from `.env.example` with a generated secret
3. ✅ Start a local PostgreSQL via Docker (if available and needed)
4. ✅ Generate the Prisma client
5. ✅ Run database migrations
6. ✅ Seed demo data (admin account + notification rules)

Then start the server:
```bash
npm run dev
```

Open **http://localhost:3000** and you're in.

### What to Do After First Launch

| Step | What | Where |
|---|---|---|
| 1 | **Log in** with the seeded account | Email: `admin@dividen.ai` / Password: `DiviDen2024!` |
| 2 | **Or create your own account** | Click "Create account" on the login page, or visit `/setup` |
| 3 | **You land in the Command Center** | The 3-panel dashboard: NOW (left), Chat/Board/CRM (center), Queue/Activity (right) |
| 4 | **Chat with Divi** | Open the Chat tab and start talking. "Create a card for Project Alpha." "Add a contact for Sarah." "Set a goal for Q3 revenue." |
| 5 | **Add your LLM key** (optional) | Settings → API Keys → add OpenAI or Anthropic key for full AI features |
| 6 | **Check system health** | Visit `http://localhost:3000/api/status` — shows DB connection, user count, migration status |

> **First-time users**: Divi walks you through a guided tour automatically. If you skip it, re-launch from Settings.

---

### Manual Setup (Step by Step)

If you prefer to run each step yourself, or the setup script doesn't work on your system:

<details>
<summary><strong>Click to expand manual instructions</strong></summary>

```bash
# 1. Clone
git clone https://github.com/Denominator-Ventures/dividen.git
cd dividen/nextjs_space

# 2. Install dependencies (pick one)
yarn install          # if you use Yarn
npm install           # if you use npm

# 3. Create environment file
cp .env.example .env

# 4. Generate a session secret and update .env
#    macOS/Linux:
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#    Copy the output and replace CHANGE_ME_GENERATE_A_REAL_SECRET in .env

# 5. Set up a database (pick one option)

#    Option A: Local Docker Postgres (easiest)
docker compose up -d
#    The default DATABASE_URL in .env already points to this.

#    Option B: Hosted Postgres (Neon, Supabase, Railway, etc.)
#    Update DATABASE_URL in .env with your connection string.

#    Option C: Local Postgres (already installed)
#    Update DATABASE_URL in .env with your local connection string.

# 6. Generate Prisma client
npx prisma generate

# 7. Run database migrations
npx prisma migrate deploy

# 8. Seed demo data
npx tsx scripts/seed.ts

# 9. Start dev server
npm run dev
```

</details>

---

### Environment Variables

| Variable | Required | Default | Description |
|---|---|---|---|
| `DATABASE_URL` | ✅ | `postgresql://dividen:dividen@localhost:5432/dividen` | PostgreSQL connection string |
| `NEXTAUTH_SECRET` | ✅ | — | Session encryption key (auto-generated by setup script) |
| `ADMIN_PASSWORD` | ✅ | `admin` | Password for the `/admin` dashboard |
| `ABACUSAI_API_KEY` | ❌ | — | For AI features. Users can also bring their own keys in Settings. |

See `.env.example` for the full template with comments.

---

### Troubleshooting

<details>
<summary><strong>@prisma/client did not initialize yet</strong></summary>

Run `npx prisma generate` — the setup script does this automatically, but if you installed manually or updated the schema, you need to regenerate.
</details>

<details>
<summary><strong>Database connection refused</strong></summary>

- If using Docker: `docker compose up -d` and wait a few seconds
- If using hosted Postgres: verify your `DATABASE_URL` in `.env` is correct and the database exists
- Test connection: `npx prisma db pull` — if this works, your connection is fine
</details>

<details>
<summary><strong>.yarnrc.yml errors</strong></summary>

Delete `.yarnrc.yml` and use npm instead. The project works with both package managers:
```bash
rm .yarnrc.yml
npm install
```
</details>

<details>
<summary><strong>I see a landing page, not the dashboard</strong></summary>

The landing page at `/` is the public marketing page. Click **"Try it"** or go directly to `/login` to sign in. After login you'll land in the Command Center dashboard.
</details>

<details>
<summary><strong>Docker — do I need it?</strong></summary>

**No.** Docker is entirely optional. We include `docker-compose.yml` as a convenience to spin up a local PostgreSQL. If you already have Postgres (local or hosted), you don't need Docker at all. Just set your `DATABASE_URL` in `.env` and skip the Docker step.
</details>

<details>
<summary><strong>Windows-specific issues</strong></summary>

- Use PowerShell (not CMD) for the setup script: `.\scripts\setup.ps1`
- If PowerShell blocks the script: `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
- All commands use `npx` which works on Windows without modification
- Docker Desktop for Windows works with the included `docker-compose.yml`
</details>

---

## What DiviDen Actually Is

DiviDen is not a dashboard. It's not a CRM. It's not another AI chat wrapper.

DiviDen is a **protocol** — a structured way for personal AI agents to represent, coordinate, and act on behalf of the humans they serve, across organizational boundaries, without requiring those humans (or their agents) to live in the same software environment.

The web application you see here is a **reference frontend**. It's one possible interface on top of the protocol. You could build another. You could build a CLI. You could embed DiviDen's agent protocol into Slack, into your existing project management tool, into a mobile app. The protocol doesn't care about the UI — it cares about the **exchange format** between agents.

### The Core Insight

Modern knowledge work has a coordination problem. Not a productivity problem — a coordination problem.

When Alice needs something from Bob, today she:
1. Figures out that Bob is the right person (often wrong)
2. Context-switches to write an email/message
3. Waits for Bob to context-switch to read it
4. Waits for Bob to context-switch again to respond
5. Context-switches again to process the response

That's 5+ context switches across 2 people for a single interaction. Multiply by every collaboration in a working day.

DiviDen replaces this with:
1. Alice's agent (her "Divi") knows what she needs
2. Her Divi knows — from profiles, skills, lived experience, task types, and availability — that Bob's Divi is the right counterpart
3. The two Divis exchange a structured **relay** — context-rich, intent-classified, priority-weighted
4. Bob's Divi triages, enriches, and presents it when Bob is ready
5. Bob's Divi responds, and Alice's Divi integrates the response into her workflow

Zero unnecessary context switches. Full context preservation. Agents doing the coordination work that humans shouldn't have to do.

---

## The Protocol Stack

DiviDen operates on four protocol layers:

### Layer 1: Identity & Profile
Each human participant has a rich identity that goes beyond job title:
- **Professional**: skills, experience, industry, current role
- **Lived Experience**: languages spoken, countries lived in, life milestones, volunteering, personal values, superpowers
- **Task Types**: self-identified categories of work they're suited for (research, introductions, technical review, mentoring, etc.)
- **Availability**: timezone, working hours, current capacity, out-of-office status
- **Privacy**: granular control over what's shared with whom

This isn't a LinkedIn profile. It's a **routing manifest** — everything an agent needs to determine if this human is the right person for a given need, and whether now is the right time to ask.

### Layer 2: Agent Relay Protocol
The structured message format agents use to communicate on behalf of their humans:
- **Types**: request, response, notification, update
- **Intents**: get_info, assign_task, request_approval, share_update, schedule, introduce, custom
- **Priority**: urgent / normal / low
- **Status lifecycle**: pending → delivered → agent_handling → user_review → completed / declined / expired
- **Threading**: relays can chain into conversations
- **Payload**: structured JSON — the agent distills only what's necessary

The relay is the atomic unit of inter-agent communication. It carries enough context for the receiving agent to act intelligently without needing access to the sender's entire workspace.

### Layer 3: Federation
Agents don't need to live on the same server. DiviDen instances federate:
- **Instance Registry**: known peers with API keys and trust levels
- **Federation Modes**: closed, allowlist, open
- **Cross-instance connections**: authenticated via shared federation tokens
- **Inbound/outbound relay routing**: relays cross instance boundaries transparently

This means Alice's company can run their own DiviDen instance, Bob's company runs theirs, and their agents still coordinate seamlessly — each instance maintaining its own data sovereignty.

### Layer 4: Integration Surface
The protocol is designed to be consumed by any environment:
- **Agent API v2**: RESTful API with Bearer token auth for external agent integration
- **Webhook Infrastructure**: calendar, email, transcript, and generic webhook types with auto-learning field mapping
- **MCP Server** (`/api/mcp`): Full JSON-RPC 2.0 implementation per MCP November 2025 spec — 6 tools, 5 resources, 2 prompts. Any MCP-compatible agent can participate in the DiviDen network.
- **A2A Protocol** (`/.well-known/agent-card.json` + `/api/a2a`): Agent Card discovery + task endpoints mapping to DiviDen relays. Compatible with Google's Agent2Agent protocol.

---

## Reference Frontend

The included Next.js application demonstrates the full protocol in action:

### Dashboard (3-Panel Layout)
- **NOW Panel**: Dynamic priority engine — urgency-scored tasks, goals, calendar gaps, focus suggestion
- **Center Panel**: 12 tabs organized into groups — Chat, Board (Kanban), CRM, Calendar, Inbox, Recordings, Drive, Connections, Teams, Goals, Jobs, Extensions
- **Queue Panel**: Agent task queue + universal activity feed with category filters and auto-refresh

### Operating Modes
- **Cockpit Mode**: Full manual control — you see everything, you control everything
- **Chief of Staff Mode**: Executive observer dashboard — daily brief, goal tracker, blocker radar, pipeline summary, calendar, comms. Full visibility, zero write access.

### Agent Intelligence (18-Layer System Prompt)
The reference agent ("Divi") operates with a dynamically-constructed system prompt covering identity, behavioral rules, conversation context, kanban/queue/CRM state, 3-tier memory, calendar, email, profile awareness, 40+ action tags, and more.

### Action System (40+ Tags)
Divi can execute structured actions via natural conversation:
`create_card`, `move_card`, `update_card`, `create_task`, `complete_task`, `create_contact`, `update_contact`, `set_mode`, `add_memory`, `recall_memory`, `dispatch_queue`, `focus_card`, `create_calendar_event`, `send_comms`, `relay_request`, `post_job`, `find_jobs`, and more.

### Additional Systems
- **Comms Channel**: Bidirectional task passing between human and agent
- **Global Search**: ⌘K command palette across all data types
- **Universal Activity Feed**: Cross-module event log with category filters and actor badges
- **Goals & NOW Engine**: Objectives with urgency scoring, merged into a prioritized stack
- **Teams & Projects**: Persistent groups with federated membership
- **Network Job Board**: Task marketplace with skill matching and reputation
- **Ambient Learning Engine**: The system learns from relay interactions and synthesizes patterns
- **Desktop Notifications**: Push notifications for blockers, relays, and agent events
- **PWA**: Install as a desktop/mobile app — no app store required

---

## Why This Matters

The AI agent landscape is converging on a few key realities:

1. **Every person will have an agent.** Not every team — every *person*.
2. **Those agents will need to talk to each other.** Not through the humans, not by sharing a platform — directly.
3. **Context is the currency.** The agent that knows its human best — their skills, their lived experience, their capacity, their preferences — provides the most value.
4. **Privacy is non-negotiable.** Agents must share only what's necessary, controlled by the human they represent.
5. **No single vendor wins.** The protocol must be open, federated, and environment-agnostic.

DiviDen is built on these realities. The protocol defines *how* agents coordinate. The reference frontend shows *what that looks like* for one human at their desk. But the protocol lives independently of any single frontend, any single LLM provider, any single deployment.

---

## API Reference

### Build Your Own Frontend / Agent
The protocol is the API. Key endpoints:

| Endpoint | Description |
|---|---|
| `POST /api/federation/connect` | Establish cross-instance connections |
| `POST /api/federation/relay` | Send/receive agent relays |
| `GET/POST /api/connections` | Manage agent connections |
| `GET/PUT /api/profile` | Manage identity profiles |
| `GET /api/v2/queue` | Get pending tasks |
| `POST /api/v2/queue/:id/result` | Report task completion |
| `GET /api/v2/kanban` | Read board state |
| `GET /api/v2/contacts` | Read CRM |
| `GET /api/v2/shared-chat/stream` | SSE real-time feed |
| `GET /api/mcp` | MCP tool discovery |
| `POST /api/mcp` | MCP tool execution |
| `GET /api/status` | Health check |

See [Protocol Specification](docs/PROTOCOL.md) and [Architecture Guide](docs/ARCHITECTURE.md) for full details.

---

## Project Structure
```
nextjs_space/
├── src/
│   ├── app/              # Next.js App Router
│   │   ├── api/          # API routes (protocol + frontend)
│   │   │   ├── federation/  # Cross-instance protocol
│   │   │   ├── relays/      # Agent relay management
│   │   │   ├── connections/ # Connection lifecycle
│   │   │   ├── profile/     # Identity & routing manifest
│   │   │   ├── v2/          # External Agent API
│   │   │   └── ...          # Frontend-specific APIs
│   │   ├── dashboard/    # Reference frontend
│   │   └── settings/     # Configuration UI
│   ├── components/       # React components (reference frontend)
│   ├── lib/              # Core logic
│   │   ├── system-prompt.ts  # 18-layer agent intelligence
│   │   ├── action-tags.ts    # 40+ executable actions
│   │   ├── now-engine.ts     # Dynamic priority scoring
│   │   ├── activity.ts       # Universal activity logging
│   │   ├── auth.ts           # Authentication
│   │   ├── prisma.ts         # Database client
│   │   └── llm.ts            # LLM integration
│   └── types/            # TypeScript definitions
├── prisma/               # Database schema + migrations
├── scripts/              # Setup scripts + seed data
│   ├── setup.sh          # One-command setup (macOS/Linux/WSL)
│   ├── setup.ps1         # One-command setup (Windows PowerShell)
│   └── seed.ts           # Database seed
├── docs/                 # Protocol & architecture docs
├── docker-compose.yml    # Local Postgres (optional)
├── .env.example          # Environment template
└── README.md             # You are here
```

---

## Standards Alignment

| Standard | DiviDen Alignment |
|----------|------------------|
| **MCP** (Model Context Protocol) | **Implemented.** Full MCP server at `/api/mcp` — 6 tools, 5 resources, 2 prompts. JSON-RPC 2.0 over Streamable HTTP. |
| **A2A** (Agent2Agent Protocol) | **Implemented.** Agent Card at `/.well-known/agent-card.json`, task endpoints at `/api/a2a`. |
| **OAuth 2.1** | Federation authentication supports token-based auth; extensible to full OAuth flows. |
| **JSON-RPC 2.0** | MCP server uses JSON-RPC 2.0 as its wire protocol. |

---

## Philosophy

> "It's not about what someone *did*. It's about what they *understand*."
> — The DiviDen approach to profiles

DiviDen captures lived experience — not just professional credentials. Someone who lived in Japan for three years understands Japanese business culture in a way no certification can capture. When Divi routes a relay, it doesn't just match skills. It matches *understanding*.

---

## License

[License TBD]

---

## Contributing

DiviDen is in active development. The protocol specification is evolving.

Key areas for contribution:
- **Protocol extensions**: new relay intents, new profile dimensions
- **Alternative frontends**: CLI, mobile, embedded widgets
- **Federation hardening**: encryption, trust scoring, reputation systems
- **Integration adapters**: Slack, Teams, Notion, Linear, GitHub

See [Architecture Guide](docs/ARCHITECTURE.md) for technical details on where to start.