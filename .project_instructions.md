# DiviDen Command Center - Project Instructions

## Overview
DiviDen is an agentic working protocol command center built with Next.js 14, Prisma, and Tailwind CSS. It provides AI agent coordination, contact management, document handling, calendar integration, and inter-agent communication.

## Design System
- Dark theme with #4F7CFF brand blue, #6B9AFF brand secondary
- Space Grotesk headings, JetBrains Mono for labels/mono text
- Background: #0a0a0a base, #111111 surface
- Text: #f5f5f5 primary, #a1a1a1 secondary, #666666 muted
- Borders: rgba(255,255,255,0.06)

## Key Architecture
- Auth: NextAuth with credentials provider, admin password from ADMIN_PASSWORD env var
- Database: Prisma with PostgreSQL, using `prisma migrate` (not `db push`) — baseline migration `0_baseline` applied 2026-04-09
- AI: Abacus.AI LLM API (ABACUSAI_API_KEY)
- Email: IMAP/SMTP integration + Abacus notification API
- Federation: Cross-instance agent communication support

## Admin Dashboard
- `/admin` route with stats, user management, system overview
- Protected by admin check (user email matches admin)

## Proactive Relay Intelligence (Agentic Communication Protocol)
- **Layer 17 system prompt** completely rewritten to instruct Divi on autonomous relay behavior
- **Three relay modes**:
  - `relay_request` — Direct ask to a specific connection's agent
  - `relay_broadcast` — Ask ALL connections at once (team-wide / company-wide)
  - `relay_ambient` — Low-priority ambient ask; receiving agent weaves it into conversation naturally without interrupting
- **Ambient protocol**: The core differentiator — receiving agents don't tell their user "someone wants to know X." They wait for the topic to come up naturally, ask about it as if they're curious, then feed the answer back via `relay_respond`
- **Response surfacing**: System prompt now includes recently completed relay responses (last 24h) with instructions to surface them naturally ("By the way, [name] got back to us about...")
- **Proactive intent detection**: Divi recognizes patterns like "I wonder what [name] thinks" → ambient relay, "ask everyone" → broadcast, "ask [name]" → direct relay
- **Smart routing**: Divi checks connection profiles (skills, task types, capacity) before choosing who to relay to
- **Chief of Staff mode**: More autonomy to proactively send ambient relays without asking first

## Outbound Invitations
- **Prisma model**: `Invitation` with token (uuid), inviter/invitee info, message, status, instanceUrl, sourceInstance, expiresAt (14 days)
- **Email**: Notification type "Connection Invitation" (NOTIF_ID_CONNECTION_INVITATION), styled HTML from `invites@{hostname}`
- **API routes**:
  - `POST /api/invites` — create + send email (or auto-create connection if user exists)
  - `GET /api/invites` — list sent invites
  - `GET /api/invites/[token]` — public peek for signup context
  - `POST /api/invites/[token]/accept` — accept specific invite
  - `POST /api/invites/accept-pending` — auto-accept all pending invites for user's email
- **Deep linking**: Setup page reads `?invite=TOKEN`, shows context, pre-fills fields, auto-login + accept after signup
- **Login page**: Reads `?invite=TOKEN`, accepts after login
- **Directory tab**: Invite form with email/name/message, sent invites list
- **Federation**: Cross-instance invites create federated connections

## Orchestration Layer (Kanban → Task → Route → Brief)
- **Brief Assembly Engine** (`src/lib/brief-assembly.ts`): Reads a Kanban card's full context graph (contacts, pipeline stage, checklist, relay history, activity) and assembles a structured Markdown brief
- **Skill Matching**: Cross-references required skills/taskTypes against connection profiles, scores by match quality + capacity
- **AgentBrief model** in Prisma: Stores reasoning receipts with source context, routing decisions, matched skills, result relay links
- **Action Tags**:
  - `task_route` — Decompose a card into tasks, match against connections, route via relay with brief attached
  - `assemble_brief` — Generate a context brief for any card without routing (show-your-work)
- **Briefs API**: `GET /api/briefs` (list by user, filterable by cardId/relayId), `GET /api/briefs/[id]` (single brief)
- **System Prompt Layer 17**: Updated with Kanban-Driven Orchestration rules (section 6) and Relay Preferences Awareness (section 7)

## Ambient Relay Learning System
- **Schema**: `AmbientRelaySignal` (per-relay outcome data: timing, quality, disruption, topic relevance) + `AmbientPattern` (synthesized cross-user learnings)
- **Engine** (`/src/lib/ambient-learning.ts`):
  - `captureAmbientSignal()` — Logs outcome metrics when relay_respond fires on ambient relay
  - `synthesizePatterns()` — Aggregates signals into patterns (timing, topic, frequency, latency, disruption, overall)
  - `getAmbientLearningPromptSection()` — Generates system prompt text from active patterns
  - `captureIgnoredAmbientSignals()` — Detects ambient relays delivered >48h without response, logs "ignored" signals
- **System prompt wiring**: Layer 17 calls `getAmbientLearningPromptSection()` and appends learned patterns
- **Self-assessment**: Layer 17 section 3b instructs Divi to include `_ambientQuality`, `_ambientDisruption`, `_ambientTopicRelevance`, `_conversationTopic`, `_questionPhrasing` in relay_respond tags for ambient relays
- **relay_respond tag syntax** (layer15) updated with self-assessment params
- **Background synthesis**: Chat flow triggers synthesis after any ambient signal capture (fire-and-forget)
- **API**: `POST /api/ambient-learning/synthesize` (triggers synthesis + ignored detection), `GET` (returns active patterns + stats)
- This is the feedback loop: signals → patterns → prompt → better ambient behavior → more signals

## Relay Preferences (User Settings)
- **New fields on UserProfile**: relayMode (full/selective/minimal/off), allowAmbientInbound, allowAmbientOutbound, allowBroadcasts, autoRespondAmbient, relayQuietHours, relayTopicFilters, briefVisibility (self/connections/public), showBriefOnRelay
- **Settings UI**: New "🌊 Relay" tab at `/settings` with full explanatory content about the ambient relay protocol
- **Preference Enforcement**: relay_ambient and relay_broadcast executors check recipient preferences before sending
- **Profile API**: Updated to handle all new boolean + JSON relay preference fields

## Teams & Projects
- **Schema**: Team, TeamMember, Project, ProjectMember models in Prisma
- **Federated members**: TeamMember/ProjectMember can use `connectionId` (federated) or `userId` (local)
- **Roles**: Team: owner/admin/member | Project: lead/contributor/reviewer/observer
- **APIs**: `/api/teams`, `/api/teams/[id]`, `/api/teams/[id]/members`, `/api/projects`, `/api/projects/[id]`, `/api/projects/[id]/members`
- **UI**: TeamsView component in dashboard center panel (Teams tab) with list/detail views for both teams and projects
- **Relay routing**: `findSkillMatches` in `brief-assembly.ts` accepts optional `teamId`/`projectId` — project members get +10, team members +5 score boost
- **Action tags**: `task_route`, `assemble_brief`, `relay_broadcast`, `relay_ambient` all accept optional `teamId`/`projectId` params
- **System prompt**: Layer 17 now fetches user's teams/projects and includes them in context, plus scoped routing instructions
- **Visibility**: Project `visibility` field: `private` (only explicit members), `team` (all team members can see), `open` (any connected user can discover)
- **Project context assembler**: `assembleProjectContext` in `brief-assembly.ts` — builds cross-member dashboard (cards, queue, relays per member)
- **Project dashboard API**: `GET /api/projects/[id]/context` — returns full project context; `?format=markdown` for prompt-ready text
- **Action tag**: `project_dashboard` — Divi can invoke to see all members' activity for a project
- **Layer 17 enhancement**: Active project dashboards (up to 3) injected into system prompt, including per-member activity and blocker detection
- **Group 11: Business Operations layer** — dynamically fetches contracts, posted jobs, applications, earnings, reputation, recordings, integrations, marketplace agents, pending applications. Only renders if user has any business activity.
- **New action tags implemented**: accept_invite, decline_invite, list_invites, complete_job, review_job, list_marketplace, execute_agent, subscribe_agent, serendipity_matches, network_briefing, route_task
- **Federation intelligence stubs**: generateNetworkBriefing, intelligentTaskRoute (brief-assembly.ts), computeSerendipityMatches (entity-resolution.ts) — graceful fallback with basic data
- **Platform Setup layer**: Navigation Reference section mapping all UI areas (Dashboard, Marketplace, Connections, Settings, Activity)
- **MarketplaceView.tsx**: JobEarningsData interface fixed to match API response structure (asWorker/asClient split)
- **Visibility enforcement**: API routes check `private`/`team`/`open` access rules; `team` visibility checks parent team membership; `open` checks any connected user
- **UI**: Projects list shows visibility badges (🔒/👥/🌐), cross-instance/cross-team indicators; project detail has "🧠 Divi's View" dashboard showing per-member activity
- **Federation docs**: `/docs/federation` has Teams & Projects section
- **KanbanCard/QueueItem/AgentRelay**: optional `projectId`; QueueItem/AgentRelay also have `teamId`

## Federation Project Context
- **Endpoint**: `GET /api/federation/project/[id]/context` — federation-facing; remote Divi can fetch cross-member project dashboards
- **Auth**: `x-federation-token` header matched against active federated Connection's `federationToken`
- **Access**: Validates connection is a ProjectMember (via `connectionId`) or connection's local user is a member
- **Reuses**: `assembleProjectContext` + `generateProjectDashboardMarkdown` from brief-assembly.ts
- **CORS**: Allows cross-origin for inter-instance calls

## Conceptual Model — Queue / Comms / Activity
- **Queue**: Where tasks are assembled between the user and their Divi (personal AI agent). This is the user's private task workspace — Divi proposes, the user approves/rejects, work gets done.
- **Comms Channel**: Where you can see your Divi interact with outside agents and other users' Divis. This is the inter-agent communication layer — federation relays, ambient asks, broadcast responses.
- **Activity Feed**: The universal event log — every event done by any user or their Divi, both inside the platform (tasks, cards, goals, chat) and outside via the comms channel (relays, federation). It's the single timeline of everything that happens.

### Activity Logging Architecture
- **Helper**: `src/lib/activity.ts` → `logActivity()` — fire-and-forget, never breaks callers
- **Wired into**: kanban (create/update/move/delete), queue (add/status/update/delete), goals (create/update/delete), contacts (add/update/delete), calendar (create/update/delete), connections (create), comms (create/reply), relays, documents, recordings, action-tags (Divi actions)
- **API**: `GET /api/activity` with query params: `actor`, `category` (queue|board|crm|calendar|goals|comms|connections|drive), `cursor`, `limit`
- **UI**: QueuePanel right sidebar has Queue/Activity tabs; Activity tab has category filter chips, auto-refresh every 30s, actor badges (You/Divi/System)

## Cockpit vs Chief of Staff Views
- **Cockpit mode** (mode='cockpit'): Full 3-column dashboard — NOW panel, CenterPanel (tabs), QueuePanel. Full header nav (search, comms, settings).
- **Chief of Staff mode** (mode='chief_of_staff'): Locked-down observer view via `ChiefOfStaffView` component. No access to full site nav (search, comms, settings hidden in header). Shows:
  - Overview tab: progress bar, stat cards, active tasks, blocked items, pending relays, recent responses, activity feed
  - Queue tab: full queue grouped by status with CoS action controls
  - Relays tab: all relay activity with status indicators
  - Activity tab: system activity feed
  - Intervention bar: send quick messages to Divi
  - Pause/Resume: bulk pause all in-progress tasks
- Auto-refreshes every 30 seconds
- Mode toggle in header switches between views; persisted via /api/settings

## System Prompt Consolidation
- Reduced from ~19 layers to ~10 consolidated groups for token efficiency
- Groups: Identity+Rules+Time | Active State (NOW+Board+Queue+Goals) | Conversation | People | Memory+Learning | Schedule+Inbox | Capabilities+Syntax | Connections+Relay | Extensions | Platform Setup

## Goals and Objectives
- Goal model: title, description, timeframe, deadline, impact, status, progress, parentGoalId (hierarchy), projectId, teamId
- APIs: GET/POST /api/goals, GET/PUT/DELETE /api/goals/[id]
- UI: GoalsView (Goals tab), action tags: create_goal, update_goal

## Dynamic NOW Engine
- now-engine.ts scores items by priority, impact, deadline, calendar gaps, relay freshness
- GET /api/now returns scored items, calendar gaps, goals summary, focus suggestion
- NowPanel rewritten with priority stack, auto-refresh every 2min

## Agent Extensions Framework
- Schema: AgentExtension model with manual/custom sources (Claw Mart removed)
- Loader: Layer 19 in system-prompt.ts
- APIs: GET/POST /api/extensions, GET/PUT/DELETE /api/extensions/[id]
- UI: ExtensionsView in dashboard, manual import only

## DEP (DiviDen Extension Proposals) Implementation
12 DEPs from FVP team fully implemented:

### New Endpoints (DEP-006/010/011/012)
- `POST /api/main-connect` — Connection Ceremony (DEP-006)
- `POST /api/main-disconnect` — Disconnection (DEP-006)
- `GET /api/a2a/playbook` — Operational Playbook (DEP-011)
- `GET /api/main-handoff` — Handoff Brief (DEP-012)
- `GET/POST /api/mcp` — MCP Server (DEP-010)

### New Libraries
- `src/lib/relay-queue-bridge.ts` — DEP-003: Bidirectional Relay↔Queue sync
- `src/lib/webhook-push.ts` — DEP-008: Webhook push events (config in ServiceApiKey service='webhook_push')
- `src/hooks/use-desktop-notifications.ts` — DEP-007: OS desktop notifications

### Wired Integrations
- DEP-001/004: Mode switch triggers wake webhook + briefing summary on return
- DEP-003: A2A tasks/respond + queue PATCH sync relay↔queue bidirectionally
- DEP-008: Webhook push wired into dispatch, queue changes, mode switch
- DEP-007: Desktop notifications in dashboard for auto-dispatch + briefing
- DEP-009: Agent card enhanced with connect/disconnect/playbook/handoff endpoints

### Schema Change
- `AgentRelay.queueItemId String? @unique` — links relay to queue item

## Federation Job Gossip (Phase B)
- `GET /api/federation/jobs` — share open network-visible jobs with federated peers
- `POST /api/federation/jobs` — ingest jobs from remote instances with deduplication
- Auth: x-federation-token header, same pattern as federation/relay
- Federated jobs appear locally with [InstanceName] prefix and source provenance
- Visibility: only jobs with visibility="network" are shared; ingested jobs get visibility="instance" to prevent re-propagation
- Federation tag embedded in description for dedup: `[federation:{instanceUrl}:{jobId}]`

## MCP Registry Submission Kit
- `public/mcp-registry/server.json` — Official MCP Registry format
- `public/mcp-registry/README.md` — Copy-paste kit for all 5 registries (Official, Smithery, PulseMCP, Glama, mcp.so)
- Agent card updated: federation.jobs endpoint added
- MCP GET discovery: federation metadata added to ecosystem block
- Federation docs page: job gossip section + flow diagram added

## FVP Integration Brief — All 14 Proposals Implemented
Full implementation of the Fractional Venture Partners integration brief:

### Tier 1: Foundation (#1-5)
- **#1 Webhook Push**: `pushRelayStateChanged` in webhook-push.ts, wired into relay PATCH + action tags
- **#2 Relay Threading**: `threadId` field on AgentRelay, auto-generate/inherit/explicit, MCP tools: relay_thread_list, relay_threads
- **#3 Structured Artifacts**: 7 typed artifact formats (text, code, document, data, contact_card, calendar_invite, email_draft) in A2A tasks/respond
- **#4 Agent Card v0.3.0**: capabilities, supportedMethods, supportedArtifactTypes, mcpTools, webhookEvents
- **#5 Entity Resolution**: `src/lib/entity-resolution.ts` + `/api/entity-resolve` + MCP tool entity_resolve

### Tier 2: Marketplace (#6-7)
- **#6 Federated Job Application**: `/api/federation/jobs/apply` — remote instances apply for local jobs
- **#7 Portable Reputation**: HMAC-signed attestations, `isFederated`/`endorsements`/`federatedScore` on ReputationScore, `/api/federation/reputation`

### Tier 3: Cross-Instance (#8-10)
- **#8 Cross-Instance MCP**: `/api/federation/mcp` — trust-level gated remote tool invocation
- **#9 Task Exchange**: `src/lib/task-exchange.ts` — auto-propose tasks to best-matched connections
- **#10 Federated Search**: `/api/federation/entity-search` — privacy-respecting cross-instance entity lookup

### Tier 4: Intelligence (#11-14)
- **#11 Pattern Sharing**: `src/lib/federation/pattern-sharing.ts` + `/api/federation/patterns` — anonymized pattern exchange with weighted merge
- **#12 Graph Matching**: `src/lib/federation/graph-matching.ts` + `/api/federation/graph` — serendipity engine (triadic closure, complementary expertise, structural bridges)
- **#13 Composite Prompts**: `src/lib/federation/composite-prompts.ts` + `/api/federation/briefing` — network briefing aggregation
- **#14 Task Routing**: `src/lib/federation/task-routing.ts` + `/api/federation/routing` — weighted 7-signal scoring model

### MCP Server v1.4.0
20 static tools: queue_list, queue_add, queue_update, contacts_list, contacts_search, cards_list, mode_get, briefing_get, activity_recent, job_post, job_browse, job_match, reputation_get, relay_thread_list, relay_threads, relay_send, entity_resolve, serendipity_matches, route_task, network_briefing
Dynamic tools: `marketplace_{slug}` for each installed marketplace agent

### Migrations
- `20260411_add_relay_threading_and_artifacts` — threadId, artifactType, artifacts on AgentRelay
- `20260411_add_portable_reputation` — isFederated, endorsements, federatedScore on ReputationScore

## Agent Marketplace (Phase 1)
- **Schema**: `MarketplaceAgent`, `MarketplaceSubscription`, `MarketplaceExecution` models
- **Migration**: `20260411_add_agent_marketplace`
- **API Routes**:
  - `GET/POST /api/marketplace` — Browse agents (filterable by category, pricing, search, sort) + Register new agent
  - `GET/PUT/DELETE /api/marketplace/[id]` — Agent detail, update, delete (owner only)
  - `POST /api/marketplace/[id]/execute` — Execute task against agent (proxy to endpoint, 30s timeout, stats tracking)
  - `POST/DELETE /api/marketplace/[id]/subscribe` — Subscribe/unsubscribe (subscription-model agents)
  - `POST /api/marketplace/[id]/rate` — Rate an execution (1-5 stars)
- **UI**: `MarketplaceView` component in dashboard CenterPanel → Network → Marketplace tab (🏪)
  - Browse view: search, category/pricing filters, sort (popular/rating/newest), grid of agent cards
  - Detail view: full info, stats, protocol badges, execute task panel with sample prompts, recent executions
  - Register view: full form for listing an agent (endpoint, auth, pricing, tags, protocols)
- **Pricing Models**: free, per_task, subscription (with optional task limits)
- **Auth Methods**: bearer, api_key, custom header, none
- **Input/Output Formats**: text, json, a2a
- **Execution proxy**: forwards requests to agent endpoint, tracks response time, success rate, updates agent stats
- **Phase 2 — Stripe Integration (LIVE)**:
  - **Stripe Connect Express** for developer payouts — onboard via hosted Stripe form, bank verification
  - **Payment methods** — consumers add cards via Stripe SetupIntent + Elements
  - **Destination charges** — on paid per_task executions, PaymentIntent with `application_fee_amount` (3% platform fee) + `transfer_data[destination]` to developer's Connect account
  - **Webhook handler** — `/api/stripe/webhooks` handles account.updated, payment_intent.succeeded/failed
  - **Schema**: User.stripeCustomerId, User.stripeConnectAccountId, User.stripeConnectOnboarded; MarketplaceExecution.stripePaymentIntentId, stripePaymentStatus
  - **API Routes**: `/api/stripe/connect/onboard`, `/api/stripe/connect/status`, `/api/stripe/connect/dashboard`, `/api/stripe/payment-methods` (GET/POST/DELETE), `/api/stripe/payment-methods/default` (PUT), `/api/stripe/status`, `/api/stripe/webhooks`
  - **UI**: PaymentSettings component in Settings → Payments tab (card management + Connect onboarding); Stripe Connect banners in Marketplace Earnings view
  - **Packages**: `stripe` (server), `@stripe/stripe-js` (client)
  - **Env vars**: `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, optional `STRIPE_WEBHOOK_SECRET`
- **Phase 1.5 — Monetization Flow + Earnings**:
  - Monetization prompts after importing extension or accepting connection (banner with prefill to marketplace register)
  - sessionStorage cross-page navigation (marketplacePrefill + openTab)
  - Prominent gradient CTAs at bottom of QueuePanel and Comms page
  - Hidden Earnings tab (visible when user has ≥1 listed agent): revenue hero, stats grid, per-agent breakdown, recent activity
  - `GET /api/marketplace/earnings` — aggregated execution stats, subscriber counts, revenue per agent
  - `GET /api/marketplace/fee-info` — public endpoint returning current fee structure

### Agent Marketplace — Revenue Split (97/3)
- **Default split**: 97% to agent developer, 3% to DiviDen as routing fee
- **Configurable**: `MARKETPLACE_FEE_PERCENT` env var (default 3), set to 0 for self-hosted instances

## Job Recruiting Monetization
- **Schema**: `JobContract` (tracks engagement terms, parties, payment history), `JobPayment` (individual payment records)
- **NetworkJob additions**: `compensationType` (flat/hourly/weekly/monthly/volunteer), `compensationAmount`, `compensationCurrency`, `isPaid`
- **Recruiting fee**: 7% default via `RECRUITING_FEE_PERCENT` env var (self-hosted: 0%)
- **Config**: `src/lib/recruiting-config.ts` — `getRecruitingFeePercent()`, `calculateRecruitingFee()`, `formatCompensation()`, `getRecruitingFeeInfo()`
- **API Routes**:
  - `POST /api/jobs/[id]/hire` — Hire applicant, create contract, initiate flat-fee payment
  - `GET /api/contracts` — List contracts (as client or worker)
  - `GET/PATCH /api/contracts/[id]` — Contract detail, status transitions (active→paused→completed/cancelled)
  - `POST /api/contracts/[id]/pay` — Record recurring payment for hourly/weekly/monthly contracts
  - `GET /api/recruiting/fee-info` — Public fee structure endpoint
- **Stripe integration**: Destination charges with application_fee_amount for recruiting fee, same pattern as marketplace
- **Webhook**: Stripe webhook handler processes `job_recruiting` payment type via metadata.type field
- **UI**: JobBoardView updated with:
  - Structured compensation form (pay structure selector + amount input)
  - Live fee preview in job creation modal
  - "📄 Contracts" tab showing all contracts with payment history
  - "✓ Hire" button in job detail modal for each pending applicant
  - ContractCard component with payment form, status management, payment history
  - Recruiting fee banner on Browse/My Posts tabs

## Terms of Service & User Agreement
- **ToS page**: `/terms` — comprehensive 14-section Terms of Service (version 1.0)
- **Key sections**: Agent liability disclaimer (Section 4), marketplace terms (Section 5), federation (Section 6), disclaimers/limitation of liability (Section 9), indemnification (Section 10), open source/self-hosted (Section 11)
- **Core disclaimer**: DiviDen is NOT responsible for any actions taken by any AI agent on behalf of any user
- **Signup agreement**: Checkbox on `/setup` page — must accept ToS before creating account. Button disabled until checked.
- **Login reference**: Small ToS link on login page ("By signing in, you agree to our Terms of Service")
- **Schema**: `acceptedTermsAt` (DateTime?) and `termsVersion` (String?) on User model
- **Both signup endpoints**: `/api/setup` and `/api/signup` accept `acceptedTerms: true` and store timestamp + version
- **Terms version**: Stored as "1.0" — can be incremented for future revisions
- **Config utility**: `src/lib/marketplace-config.ts` — `getPlatformFeePercent()`, `calculateRevenueSplit()`, `getFeeInfo()`
- **Schema fields (MarketplaceAgent)**: `totalGrossRevenue`, `totalPlatformFees`, `totalDeveloperPayout`, `pendingPayout`
- **Schema fields (MarketplaceExecution)**: `grossAmount`, `platformFee`, `developerPayout`, `feePercent`
- **Execution flow**: On successful paid execution, calculates split, stores on execution + increments agent accumulators
- **Earnings dashboard**: Shows gross vs net breakdown, per-agent revenue, pending payouts, fee info with self-hosted note
- **Registration form**: Shows live fee calculation preview when listing paid agents
- **Migration**: `20260411_add_marketplace_revenue_split`

### Agent Install/Uninstall System + Integration Kit
- **Schema**: MarketplaceSubscription gains `installed Boolean`, `installedAt DateTime?`, `uninstalledAt DateTime?`, index on installed
- **Migration**: `20260411_add_agent_install_management`
- **API**: `POST/DELETE /api/marketplace/[id]/install` — install loads Integration Kit into MemoryItem entries (`agent:{agentId}:identity|task_types|context_instructions|etc`), uninstall deletes them
- **System prompt (Layer 11)**: Only loads agents where `installed: true`, tells Divi to suggest `[[install_agent:...]]` for non-installed
- **Action tags**: `install_agent`, `uninstall_agent` — full memory management (same logic as API endpoint)
- **UI (MarketplaceView)**: Install/Uninstall toggle button in detail view, green "Installed" badge in header, pulse indicator
- **MCP (DEP-010)**: `tools/list` dynamically adds installed agents as `marketplace_{slug}` tools; `tools/call` proxies to agent endpoint for marketplace-prefixed tools. Version bumped to 1.4.0
- **A2A Agent Card**: Skills array dynamically extended with installed marketplace agents; mcpTools list includes `marketplace_{slug}` entries
- **Memory key pattern**: `agent:{agentId}:identity`, `:task_types`, `:context_instructions`, `:preparation_steps`, `:input_schema`, `:output_schema`, `:usage_examples`, `:execution_notes` — category `agent_toolkit`, tiers 1-3

## Deployment
- Production: dividen.ai (custom domain) and sdfgasgfdsgsdg.abacusai.app
- Both untagged — deploy once updates both
- Admin password: DiviDenAdmin2026! (ADMIN_PASSWORD env var)
- User email: jon@fractionalventure.partners
- GitHub: Denominator-Ventures/dividen (PAT stored in environment, not committed)
## Git Workflow — STRICT RULES
- **ALWAYS pull before push**: `git pull --rebase origin main` BEFORE `git push origin main`. No exceptions.
- **Never overwrite LICENSE**: The LICENSE file is `MIT, Copyright (c) 2024-2026 Denominator Ventures`. Do not modify it unless Jon explicitly asks.
- **Never commit .env**: The `.gitignore` protects `.env` — never `git add .env` or `git add -A` without verifying `.env` isn't staged. The `.env.example` is the public template.
- **Never force push**: Always use `git pull --rebase` to integrate remote changes, never `git push --force`.
- **Commit messages**: Use conventional format — `feat:`, `fix:`, `docs:`, `chore:`. Include what changed, not just "update".
- **Protected files** (never modify without explicit request): `LICENSE`, `.env`, `.env.example`, `docker-compose.yml`, `.gitignore`
- **Pre-push checklist**: 1) `git status` — verify no secrets staged, 2) `git pull --rebase origin main`, 3) resolve any conflicts, 4) `git push origin main`