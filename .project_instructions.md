- Legacy onboarding phases (0-5) **removed from system prompt** — old-flow users figure it out on their own. `onboardingPhase` field still exists in DB but is not read by system-prompt.ts.

## System Prompt — Modular Capability Architecture (Phase 1)
- **File**: `src/lib/system-prompt.ts`
- **Philosophy**: "Capabilities as installed modules" — every Divi ships with core, optional modules load on-demand by relevance scoring.
- **Old**: Monolithic `buildCapabilitiesAndSyntax()` (7,219 tokens) loaded on EVERY message.
- **New**: 5 modular functions, only `capabilities_core` always loads:
  - `buildCapabilitiesCore()` (~3,200 tokens, ALWAYS) — Card CRUD, Checklist, People, Artifacts, Queue, Goals, Profile/Memory/Setup tags, Interactive Widgets, Linked Kards, Continuous Task Awareness, Core Operating Principles
  - `buildTriageCapabilities()` (~1,200 tokens, ON-DEMAND) — Full 8-step triage protocol, outbound capabilities. Signals: triage, catch-up, morning, signals, email, transcript, comms.
  - `buildRoutingCapabilities()` (~800 tokens, ON-DEMAND) — Task detection, routing waterfall, task board, orchestration, invite intake. Signals: route, assign, delegate, who should, relay, connection.
  - `buildFederationCapabilities()` (~200 tokens, ON-DEMAND) — Entity resolve, serendipity, route_task, network_briefing. Signals: federation, cross-instance, remote, network.
  - `buildMarketplaceCapabilities()` (~200 tokens, ON-DEMAND) — list/execute/subscribe/install/uninstall agent. Signals: marketplace, agent, install, subscribe.
- **Relevance engine**: `PromptGroup` type has 17 groups (was 13). `SIGNAL_PATTERNS` regex map drives `scoreGroupRelevance()`. Groups with empty patterns (like `capabilities_core`) always score 1.0.
- **Setup layer**: `buildSetupLayer_conditional` no longer takes `onboardingPhase`. "Everything configured" path is ~200 tokens (status line + nav ref). "Incomplete" path has widget mappings only.
- **Dead code removed**: `layer16_platformSetupAssistant_optimized` (never called), duplicate Federation Intelligence section.
- **Token savings**: ~5,000-6,000 tokens saved per non-triage/non-routing message.
## CapabilityModule Phase 2 (SHIPPED — v1.6.1)
- **File**: `src/lib/capability-module.ts`
- **Interface**: `CapabilityModule` — formal contract for marketplace capabilities integrating with the relevance engine.
- **Schema additions** to `MarketplaceCapability`: `signalPatterns` (JSON array of regex strings), `tokenEstimate` (int), `alwaysLoad` (bool), `moduleVersion` (string, default "1.0"), `totalGrossRevenue`, `totalPlatformFees`, `totalDeveloperPayout`.
- **Scoring**: `scoreCapabilityModule()` uses same weights as static groups — message match (+0.6), context match (+0.3), baseline (+0.05), threshold (0.3). `alwaysLoad=true` → 1.0.
- **Loading**: `loadRelevantCapabilityModules(userId, message, context)` fetches installed caps, scores them, returns only those above threshold.
- **Prompt injection**: `buildCapabilityModulePrompt()` formats scored modules with metadata for system prompt.
- **System prompt integration**: Injected as Group 14 in `buildSystemPrompt()`. Each module loaded independently — no longer dumped under generic `active_caps`.
- **Revenue**: 97/3 split (same as agents) applied to paid capability purchases via `calculateRevenueSplit()`.
- **API**: `GET /api/v2/prompt-groups` — exposes all 17 static groups, signal patterns, scoring params, CapabilityModule spec. For federation alignment.
- **Webhook**: `POST /api/marketplace/webhook` — receives `agent_approval` events from managed marketplace. Validates `X-Federation-Token`.
- **Latest commit**: `9d638da` on main — v1.9.3 update (interactive widgets, developer kit, Bubble Store).

## Bubble Store (v1.9.3 → v1.9.4)
- **Branding**: All user-facing "Marketplace" → "Bubble Store", 🏪 → 🫧. Internal IDs/routes/file names unchanged.
- **Project Bible**: `src/docs/dividen-project-bible.md` — comprehensive onboarding document for new development conversations.

## v1.9.4 Sprint (shipped)
- **Bubble Store Rebrand**: All user-facing copy updated across ~20 files. Historical changelog entries left untouched.
- **Agent Approval Pipeline Fix**: Material change detection on federation agent updates (10 fields compared). Active agents flip to `pending_review` on material change, version auto-increments, changelog with diff appended. Developers + existing subscribers retain access while pending. Detail view shows version history with expandable diffs.
- **Inbox Zero Automation**: QueuePanel auto-categorizes items (action/notification/relay/fyi/task), shows colored badges. Batch mode with select-all, complete-all, snooze-all. Quick-clear for notifications. Celebratory inbox zero state.
- **Ambient Learning Pipeline**: `synthesizeBehaviorLearnings(userId)` in `ambient-learning.ts` — clusters BehaviorSignals into 6 pattern types (schedule, day-of-week, workflow style, decision speed, capability usage, queue triage). Creates/updates UserLearning records. Surfaces high-confidence learnings as queue suggestions (max 1/24h). Auto-triggers every 25th signal via behavior-signals POST. Wired into `/api/ambient-learning/synthesize` POST alongside existing relay pattern synthesis.

## Widget Library (v1.9.2)
- **Directory**: `src/components/widgets/` — theme-agnostic, reusable widget primitives.
- **Theme**: `widget-theme.css` defines CSS custom properties (`--widget-bg`, `--widget-accent`, `--widget-border`, etc.). DiviDen defaults set; override one CSS file to re-theme.
- **Primitives**: WidgetSlider, WidgetToggle, WidgetRadio, WidgetSelect, WidgetTextInput, WidgetInfo, WidgetGoogleConnect, WidgetWebhookSetup, WidgetSubmitButton, WidgetSkipButton.
- **Agent system**: AgentWidget, AgentWidgetContainer, parseWidgetPayload — moved from dashboard/AgentWidget.tsx, fully themed via CSS vars.
- **Barrel export**: `@/components/widgets` — import everything from here.
- **OnboardingChatWidgets**: Rewritten from 552 → 190 lines, delegates to widget library.
- **Consumers**: ChatView, QueuePanel import from `@/components/widgets`.
- **Design goal**: Decouple from DiviDen's design system. Open-source community can override `widget-theme.css` for any theme.

## Onboarding Setup Flow (v1.9.0)
- **Single source of truth**: `SETUP_TASK_ACTIONS` in `src/lib/onboarding-project.ts` maps each setup task to either an `actionTag` (for widget tasks) or an `agentPrompt` (for conversational tasks).
- **No LLM dependency for widget rendering**: When a task is completed, the advance endpoint returns `nextTaskAction` with the action config. The client renders "⚡ Yes, let's go" / "⏭️ Skip for now" buttons via `SetupNextTaskButtons` component.
- **Direct widget triggering**: Clicking "Yes" calls `/api/chat/execute-tag` (restricted to `show_settings_widget` and `show_google_connect`) which returns widget data. The client renders the widget inline — no LLM round-trip.
- **Google Connect flow**: After OAuth callback, `google-connect/route.ts` marks the task complete and injects an auto-continue message with `isSetupNextTask` metadata. Dashboard refreshes chat on return.
- **System prompt simplified**: No longer contains hardcoded task→widget mapping table. Just general instructions that the UI handles setup flow automatically.
- **"Together" path**: First task widget is triggered directly via `handleSetupNextTask`, not via LLM message.
- v1.8.2: Release notes v1.6.1→v1.8.1, docs audit fixes on /developers /documentation /release-notes
- Previous: `1e7c28b` (onboarding project at signup), `a867dd8` — v1.6.1 (marketplace webhook, revenue split, prompt-groups API, CapabilityModule interface)

## Federation v1.8.0 — P3 Shipped
- **NEW endpoint**: `POST /api/v2/federation/capabilities` — Sync capabilities from federated instances. Mirrors agents pattern. Accepts `promptGroup`, `signalPatterns`, `tokenEstimate`, `alwaysLoad`.
- **NEW endpoint**: `GET /api/v2/federation/capabilities` — List federated capabilities for an instance.
- **Schema**: Added `promptGroup`, `sourceInstanceId`, `sourceInstanceUrl`, `remoteCapabilityId` to `MarketplaceCapability`.
- **Federated developer profiles**: `/developer/[slug]` page shows developer name, instance origin (purple "Federated via X" badge), all their agents + capabilities on DiviDen.
- **Developer links**: Federated agents → `/developer/{slug}` (purple text), Platform agents → `/profile/{developerId}` (brand color). Applied across Marketplace, Discover, Search, Directory, Connections.
- **Naming conventions (canonical)**: `per_task` (not `per_execution`), `pending_review` (not `pending_approval`), `pricePerTask` (accepts `pricingAmount` as alias), `currency` field (default USD).
- **No separate developer profile fields**: Just `developerName` + `developerUrl`. No bio/title/company/industry stored.
- **Jon's preference**: "developer name that opens their profile to connect with them on divi" — no corporate profile fields.