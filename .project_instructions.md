## HowItWorks Section
- **File**: `src/components/landing/HowItWorks.tsx` — Named export `{ HowItWorks }`
- **Placement**: After Hero section, before Problem/Solution section in `LandingPage.tsx`
- **Features**: 5-step animated flowchart (Signals → Divi Brain → Board → You → Agents), auto-play highlighting every 3s, intersection observer fade-in, responsive (3+2 grid desktop, vertical mobile), progress dots
- **Animations**: `animate-flow-right` and `animate-flow-down` keyframes added to `tailwind.config.ts`

## Onboarding Flow (Revised)
- **No more wizard walkthrough** — removed `Walkthrough.tsx` and `OnboardingWizard.tsx` from dashboard flow
- **Welcome popup**: `src/components/dashboard/OnboardingWelcome.tsx` — shown to new users, single "Start Chat with Divi" button
- **Chat-first onboarding**: Divi introduces itself in chat, then guides through phases (settings, email connect, signals, etc.)
- **Empty states**: NowPanel and QueuePanel show empty placeholder during onboarding (phases 1-5), filled once signals connected
- **Tab badges**: CenterPanel shows badge counts for Email (unread), Calendar (events today), Drive (file count) — refreshed every 60s
- **Onboarding resume**: If user navigates away from chat during onboarding, a notification is created to resume; re-login also resumes
- **Chat alignment**: User messages RIGHT (brand-colored bubble), Divi messages LEFT (surface-colored bubble with avatar)